class Solution {
    public boolean rotateString(String s, String goal){
        if(s.length()!=goal.length()){
            return false;
        }
       String ans=s+s;
       return (s.length()==goal.length() && ans.contains(goal));
    }
}
